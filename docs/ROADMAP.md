# ROADMAP.md

Roadmap only. Nothing here is implemented until its row is marked
implemented **and verified** in `CLAUDE.md`. Per `AGENTS.md` §1, roadmap
items must never be described as working features in code, docs, or agent
responses.

## Phases

| Phase | Deliverable | Exit criterion (verified) |
|---|---|---|
| 0 | Scaffolding | Repo cloned, AGENTS.md and CLAUDE.md customized for this project, scripts/{setup,build,test}.sh runnable |
| 1 | `<first real feature>` | `<concrete verifiable exit criterion>` |

## Explicitly out of scope (now)

<!-- Things that aren't on the roadmap but might be assumed. State them so
     they don't accidentally drift into scope. -->

-

## Deferred ideas (not commitments)

<!-- Interesting but unscheduled. Listed here for context, not as promises. -->

-
